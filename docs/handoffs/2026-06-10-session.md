# Session handoff — 2026-06-10

> Worker repo: `fissible/attest`. Touched this session: `attest`, `attest-laravel`, `station`,
> `fissible/.github`. This was a long session that started as crash recovery and ended with
> **attest v1.0.0 shipped**.

## Headline: `fissible/attest` 1.0.0 is released

- **`attest` v1.0.0** — first stable release. Tag `v1.0.0` on `main`, CI + Release workflows
  green, GitHub Release published, VERSION/tag aligned.
- **`attest-laravel` v0.4.1-alpha** — now requires `fissible/attest ^1.0` (was `^0.4.0-alpha`);
  green against 1.0.0 (181 tests, PHPStan clean), released.

## What happened, in order

1. **Crash recovery.** Identified that the pre-crash work was adapter **Chunk 6 (JSONL importer)**,
   already shipped as `attest-laravel v0.4.0-alpha`; wrote the missing recovery handoffs
   (`docs/handoffs/2026-06-08-session.md` in both repos).
2. **Layered READMEs.** Rewrote core + adapter READMEs with plain-language → technical progressive
   disclosure and worked record+verify examples.
3. **v1.0 roadmap.** Scoped the path to 1.0 (`docs/plans/2026-06-08-v1.0-roadmap.md`). Two locked
   decisions: **Path A** (local integrity stable at 1.0; anchoring ships `@experimental`) and
   **switch the binary wrapper to `$binary`**.
4. **Executed T1–T6** (GitHub issues #2–#7, all closed):
   - **T1** `$binary` canonical wrapper + reserved-key guard in `PayloadValidator` (breaking format
     change; `_attest_binary` retired).
   - **T2** optional `fsync` flag on `FileChainStore` (default off; PHP 8.1+ `fsync()`).
   - **T3** API-stability pass: `STABILITY.md` + annotated **37 `@api` / 23 `@internal`** classes.
   - **T4** anchoring `@experimental` carve-out: **39 classes** (`src/Anchor`, `src/Headers`,
     `src/Merkle`, + anchoring verification types). Every src class is now tagged.
   - **T5** aligned the `station` design spec (§5.3/§6/§7.1/§11) → **PR `station#399`**.
   - **T6** cut **v1.0.0** (pre-flight: no binary-bearing data in the suite; gate green).
5. **Downstream:** bumped `attest-laravel` to require attest `^1.0` (+ dev path-repo pin → `1.0.0`),
   released `v0.4.1-alpha`.
6. **Node 20 CI fix:** bumped `actions/checkout@v4`→`v5` (and `actions/cache@v4`→`v5` in attest)
   across `attest`, `attest-laravel`, and the `fissible/.github` reusable workflows. CI verified
   green with v5. `shivammathur/setup-php@v2` left as-is (Node-24-ready).
7. **README refresh for 1.0:** flipped core status Alpha → Stable (v1.0+), pointed docs at
   `STABILITY.md`, updated CLI-schema pin v0.4.x → 1.x; adapter README noted "built on stable
   attest 1.0 core."
8. **NMS scoping (new direction):** drafted `docs/integrations/2026-06-09-nms-laravel-attest-integration.md`
   — a Laravel network-management system on attest-laravel (per-device chains, signed verification
   reports as bundle + PDF). This is the first **adversarial-audience** use case and is the trigger
   that would justify graduating anchoring (Path B). Captured in memory `attest-nms-consumer`.

## Current state

- `attest` `main` clean, aligned, at **v1.0.0**.
- `attest-laravel` `main` clean, aligned, at **v0.4.1-alpha**.
- All v1.0 issues (#2–#7) closed.

## Next tasks / open follow-ups

1. **Merge `station#399`** (`docs/attest-spec-v1-align` → station `main`) — doc-only spec alignment.
2. **Anchoring graduation (Path B)** — deferred; do the live-network soak (real OTS calendars +
   Bitcoin confirmations) when the NMS use case firms up. Would move anchoring `@experimental` → `@api`.
3. **NMS build** — if pursued, follow the integration design note; it's a new (not-yet-created) repo,
   so it's a PM/portfolio decision.

## Caveats / things I deliberately did NOT touch

- **`station` is on branch `chore/tagged-module-deps`** with **uncommitted WIP** (`composer.json`,
  `composer.lock`, untracked `docs/`). This is pre-existing work, not mine. My T5 commit was
  relocated off it (to PR #399) via `reset --mixed` so the WIP was preserved.
- **`fissible/.github` has uncommitted MCP WIP** (`mcp/server.py`, `mcp/tests/test_server.py`,
  untracked `docs/`). My checkout-v5 commit there was scoped to only the two workflow files; the
  MCP WIP is untouched and is yours to finish.
- **CI housekeeping (org-wide, dated):** GitHub forces Node 24 ~2026-06-16 and removes Node 20
  ~2026-09-16. The `checkout`/`cache` bumps handle attest + the reusable workflows; other fissible
  repos may still need the same bump.

## Useful references

- Roadmap: `docs/plans/2026-06-08-v1.0-roadmap.md` (status table + follow-ups)
- Stability policy: `STABILITY.md`
- NMS integration note: `docs/integrations/2026-06-09-nms-laravel-attest-integration.md`
- Spec: `~/lib/fissible/station/docs/superpowers/specs/2026-05-25-fissible-attest-design.md`
  (aligned via PR station#399)
